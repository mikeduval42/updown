<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= content_for?(:title) ? yield(:title) : "foundation-rails" %></title>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
    <!-- <link href='http://fonts.googleapis.com/css?family=Playball' rel='stylesheet' type='text/css'>
    <link href='http://fonts.googleapis.com/css?family=Poiret+One' rel='stylesheet' type='text/css'> -->
    <link href='http://fonts.googleapis.com/css?family=Nothing+You+Could+Do|IM+Fell+English' rel='stylesheet' type='text/css'>

  </head>

  <body>
    <%= javascript_include_tag "application" %>
    <div class="contain-to-grid sticky">
      <nav class="top-bar" data-topbar data-options="sticky_on: large">
        <ul class="title-area">
          <li class="name">
            <h1><a href="#">UpDown</a></h1>
          </li>
           <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
          <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
        </ul>

        <section class="top-bar-section">
          <!-- Right Nav Section -->
          <ul class="right">
            <li class="active"><a href="/outfits">Show an Outfit!</a></li>
            <li class="has-dropdown">
              <a href="#">Menu</a>
              <ul class="dropdown">
                <% if current_user %>
                <li><%link_to "My Outfits", outfits_path%></li>
                <li><a href="#">Friends</a></li>
                <li><%= link_to "Sign Out", sessions_path, :method => :delete %></li>
                <% else %>
                <li><%= link_to "Sign In", new_sessions_path%></li>
                <li><%=link_to "Sign Up", new_user_path%></li>
                <%end%>
              </ul>
            </li>
          </ul>

          <!-- Left Nav Section -->
          <!-- ul class="left">
            <li><a href="#">Left Nav Button</a></li>
          </ul> -->
        </section>
      </nav>
    </div>
    <%= yield %>
  </body>
</html>